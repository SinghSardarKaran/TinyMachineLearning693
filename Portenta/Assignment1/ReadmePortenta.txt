Imported the python library pyb and defined the red, green and blue LED's

and gave variables such as greenLED , blueLED ,redLED 

later on used while loop for delaying function to alternate on and off switches between led's and delay selected was 500

Snapshot:
Imported the libraries sensor, image and pyb 

import sensor, image, pyb

Then, at that point, initailized camera sensors and set a complete deferral or casing skip of 4000 ms
utilized the snapshot function to take a picture and save to save the picture

